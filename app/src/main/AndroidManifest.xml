<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lingc.zhihudaily">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activities.SettingsActivity"></activity>
        <activity
            android:name=".view.activities.AboutActivity"
            android:theme="@style/AppTheme.MaterialAboutActivity" />
        <activity android:name=".view.activities.SectionActivity" />
        <activity android:name=".view.activities.PostActivity" />
        <activity
            android:name=".view.activities.ExceptionActivity"
            android:label="程序捕捉到异常" />
        <activity
            android:name=".view.activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>